## Production Loader

The loader automatically scans the configured knowledge directory and loads all supported PDF documents.

Every loaded document preserves metadata and is forwarded to the chunking stage.